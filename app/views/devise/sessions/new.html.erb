<div class="view-container flex">
  <nav class="navbar login-navbar">
    <div></div>
    <span class="nav-header">LOGIN</span>
    <div>
      <input class="login-button white-text" type="submit" name="commit" value="Log in" data-disable-with="Log in" form="new_user">
    </div>
  </nav>
  <div class="content-list flex">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "list-item flex login-form" }) do |f| %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" , required: true, placeholder: "Email", class: "login-input" %>
    <%= f.password_field :password, autocomplete: "current-password" , required: true, placeholder: "Password", class: "login-input" %>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>

</div>